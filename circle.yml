dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install clang-3.6
deployment:
  tag:
    tag: /v[0-9]+(\.[0-9]+)*(-.*)*/
    owner: bep
    commands:
      - gem install fpm
      - export CC="clang-3.6"
      - curl -sL https://git.io/goreleaser | bash